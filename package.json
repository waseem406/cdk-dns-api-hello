{
  "name": "domain-management",
  "version": "0.1.0",
  "bin": {
    "domain-management": "bin/domain-management.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.106.1",
    "aws-cdk": "1.106.1",
    "jest": "^26.4.2"
  },
  "dependencies": {
    "@aws-cdk/core": "1.106.1"
  }
}
